package helpers;

// the paths for all the files
public class Paths {

    static private final String base = "src/Files";  // if there is an error delete src/

    static public String adminPath = base + "/admin.txt";

    static public String lecturersPath = base + "/lecturers.txt";

    static public String studentsPath = base + "/students.txt";

    static public String subjectsPath = base + "/subjects.txt";

    static public String studentCoursesPath = base + "/StudentsCourses/std_";

    static public String lecturerCoursesPath = base + "/LecturersCourses/lec_";

    static  public  String  examPath = base +   "/Exams/sub_";
}
